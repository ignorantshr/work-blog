编写脚本`vix.sh`：
```shell
#!/bin/bash
filename=$1
touch $filename
chmod +x $filename
vim $filename
```

链接到`/usr/local/sbin`下：

```shell
mv vix.sh /usr/local/sbin/vix.sh
ln /usr/local/sbin/vix.sh /usr/local/sbin/vix
```

